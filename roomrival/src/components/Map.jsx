import React from 'react';

const Map = ({ username }) => {
    return (
        <div>
            <h1>Welcome, {username}!</h1>
            {/* Rest of your map page content */}
        </div>
    );
};

export default Map;